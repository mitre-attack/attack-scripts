import pandas as pd

def techniquesToDf(stix_data, include_deprecated=False):
    """convert the stix techniques to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
def tacticsToDf(stix_data, include_deprecated=False):
    """convert the stix tactics to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
def softwareToDf(stix_data, include_deprecated=False):
    """convert the stix software to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
def groupsToDf(stix_data, include_deprecated=False):
    """convert the stix groups to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
def mitigationsToDf(stix_data, include_deprecated=False):
    """convert the stix mitigations to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
def matricesToDf(stix_data, include_deprecated=False):
    """convert the stix matrices to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
def relationshipsToDf(stix_data, include_deprecated=False):
    """convert the stix relationships to pandas dataframes. Return a lookup of labels (descriptors) to dataframes"""
    pass
